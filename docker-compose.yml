version: '3.8'
services:
  # MySQL Database
  mysql:
    image: mysql:8.0
    container_name: traffic-buster-db
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: market_db
    ports:
      - "3306:3306"
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    volumes:
      - ./mysql-init:/docker-entrypoint-initdb.d

  # Redis (Cache & Lock)
  redis:
    image: redis:alpine
    container_name: traffic-buster-redis
    ports:
      - "6379:6379"

  # Application Instance 1
  app-1:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - mysql
      - redis
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/market_db?useSSL=false&allowPublicKeyRetrieval=true
      - SPRING_DATASOURCE_USERNAME=root
      - SPRING_DATASOURCE_PASSWORD=root
      - SPRING_DATA_REDIS_HOST=redis
      - SPRING_DATA_REDIS_PORT=6379
      - SPRING_PROFILES_ACTIVE=redis
      - SPRING_JPA_HIBERNATE_DDL_AUTO=validate

  # Application Instance 2
  app-2:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - mysql
      - redis
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/market_db?useSSL=false&allowPublicKeyRetrieval=true
      - SPRING_DATASOURCE_USERNAME=root
      - SPRING_DATASOURCE_PASSWORD=root
      - SPRING_DATA_REDIS_HOST=redis
      - SPRING_DATA_REDIS_PORT=6379
      - SPRING_PROFILES_ACTIVE=redis
      - SPRING_JPA_HIBERNATE_DDL_AUTO=validate

  # Nginx Load Balancer
  nginx:
    image: nginx:latest
    container_name: traffic-buster-lb
    ports:
      - "8080:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - app-1
      - app-2
